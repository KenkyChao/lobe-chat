'use client';

import { memo } from 'react';

const TelemetryNotification = memo<{ mobile?: boolean }>(() => null);

TelemetryNotification.displayName = 'TelemetryNotification';

export default TelemetryNotification;
